# FastRoute router adapter

This package provides a FastRoute adapter for the ellipse router.

**Require** php >= 7.1

**Installation** `composer require ellipse/router-fastroute`

**Run tests** `./vendor/bin/kahlan`
